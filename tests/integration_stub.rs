// Stub integration test for the vault factory contract.
// Extend this with real scenarios using near-workspaces when ready.
#[tokio::test]
async fn integration_stub() -> Result<(), Box<dyn std::error::Error>> {
    // TODO: implement integration tests for vault factory (e.g., deploy, call new, mint_vault)
    Ok(())
}
